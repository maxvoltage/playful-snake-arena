services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: snake-app
    # No DATABASE_URL provided, defaults to SQLite inside the container
    ports:
      - "80:80"
    networks:
      - snake-network

networks:
  snake-network:
    driver: bridge
